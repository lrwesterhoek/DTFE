For install information see README.md ("Installation and Building"), or run './install.sh'
for a one-command build. The PDFs in './documentation/' cover the original DTFE code and
predate this fork's build system (GPU backends, PS-DTFE, install scripts).
